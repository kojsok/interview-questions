let, const и var — это ключевые слова в JavaScript, используемые для объявления переменных, но они обладают разными свойствами и поведением.

1. var
Область видимости (Scope): Переменные, объявленные с помощью var, имеют функциональную область видимости (function scope). Это означает, что они доступны во всей функции, в которой объявлены, а не ограничиваются блоками кода (например, циклами или условными операторами).
Поднятие (Hoisting): Переменные, объявленные с var, поднимаются в начало их области видимости. То есть, они могут быть использованы до того, как были объявлены, но их значение будет undefined до момента присвоения.
Повторное объявление: Переменные, объявленные с var, могут быть переобъявлены в той же области видимости без ошибки.
2. let
Область видимости (Scope): Переменные, объявленные с let, имеют блочную область видимости (block scope). Они доступны только в пределах блока кода (например, в {}), в котором были объявлены.
Поднятие (Hoisting): Хотя переменные, объявленные с let, тоже поднимаются, они не могут быть использованы до объявления. Это называется "временной мертвой зоной" (temporal dead zone).
Повторное объявление: В одной и той же области видимости переменные, объявленные с let, не могут быть переобъявлены.
3. const
Область видимости (Scope): Так же, как и let, переменные, объявленные с const, имеют блочную область видимости.
Поднятие (Hoisting): Поведение с поднятием аналогично let. Переменные не могут быть использованы до того, как они будут объявлены.
Константность: const используется для объявления переменных, значение которых не может быть изменено после присвоения. Однако это не означает, что объект или массив, объявленный через const, будет неизменяемым. Вы можете изменять свойства объектов или элементы массива, но не можете переназначить саму переменную.
Повторное объявление: В одной и той же области видимости переменные, объявленные с const, не могут быть переобъявлены.
Пример
javascript
Копировать код
// var
var x = 10;
if (true) {
    var x = 20; // та же переменная
    console.log(x); // 20
}
console.log(x); // 20

// let
let y = 10;
if (true) {
    let y = 20; // другая переменная
    console.log(y); // 20
}
console.log(y); // 10

// const
const z = 10;
if (true) {
    const z = 20; // другая переменная
    console.log(z); // 20
}
console.log(z); // 10

// Пример с объектом
const obj = { name: "John" };
obj.name = "Jane"; // Можно изменить свойство объекта
// obj = {}; // Ошибка: нельзя переназначить переменную obj
Резюме
Используйте var, если требуется функциональная область видимости (но в современном JavaScript это редко нужно).
Используйте let для переменных, которые могут изменяться и которые должны быть ограничены блочной областью видимости.
Используйте const для переменных, значение которых не должно изменяться (например, константы).
